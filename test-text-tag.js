
import Lightview from './src/lightview.js';
const { state, tags, effect } = Lightview;

// Mock document for testing in Node if necessary, but this project seems to have tests.
// Let's check how tests are run.
